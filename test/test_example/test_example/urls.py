from django.conf.urls import include, url
from django.contrib import admin

urlpatterns = [
    # Examples:
    # url(r'^$', 'test_example.views.home', name='home'),
    # url(r'^blog/', include('blog.urls')),

]
